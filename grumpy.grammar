program 
    -> var_assignment  {% id %}
    | number           {% id %}

var_assignment -> identifier "->" number

identifier -> [A-zA-Z]:+ {% id %}

number 
    -> digits {% 
        data => Number(data[0])
    %} 
    | digits "." digits {%
        data => Number(data.join(""))
    %}

digits -> [0-9]:+ {%
    data => data[0].join("")
%}